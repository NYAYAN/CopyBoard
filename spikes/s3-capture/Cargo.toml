[package]
name = "s3-capture"
version = "0.1.0"
edition = "2021"

[dependencies]
xcap = "0.9"
image = { version = "0.25", default-features = false, features = ["png", "jpeg"] }

[profile.release]
opt-level = 3
